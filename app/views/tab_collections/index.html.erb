<h1>みんなのリンク集</h1>
<% @all_links.each do |link| %>
  <ul>
    <li><%= link_to my_link_path(link) do %><%= link.title %><% end %></li>
    <li><%= link.description %></li>
    <li>★<%= link.favorites.count %> ストックされている数</li>
  </ul>

<% end %>
